import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rswqrcodegenerator.app',
  appName: 'QR Code Gen',
  webDir: 'dist/rsw-qr-code-generator-app/browser'
};

export default config;
